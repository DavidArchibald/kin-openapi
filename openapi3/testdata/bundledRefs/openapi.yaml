openapi: 3.1.0
info:
  title: bundled refs
  version: "1.0.0"
paths: {}
components:
  schemas:
    Outer:
      type: object
      unevaluatedProperties: false
      properties:
        fromCommon:
          $ref: 'common.yaml#/Item'
        fromOther:
          $ref: 'other.yaml#/Item'
