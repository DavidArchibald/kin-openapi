invalid paths: invalid path /v1/teams/{teamId}/members/{uid}: invalid operation PATCH: invalid default: got array, want string
Schema:
  {
    "default": [
      "MEMBER",
      "VIEWER"
    ],
    "description": "The role in the team of the member.",
    "examples": [
      [
        "MEMBER",
        "VIEWER"
      ]
    ],
    "type": "string"
  }

Value:
  [
    "MEMBER",
    "VIEWER"
  ]
